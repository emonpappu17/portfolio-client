import React from 'react';
import { HeroHeader } from '../header';

const Navbar = () => {
    return (
        <div>
            <HeroHeader></HeroHeader>
        </div>
    );
};

export default Navbar;